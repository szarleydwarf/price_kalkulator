<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blue_d</item>
        <item name="colorPrimaryDark">@color/blue_l</item>
        <item name="colorAccent">@color/white</item>
    </style>

    <style name="MyDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:textColor">@color/yellow</item>
        <item name="android:textColorPrimary">@color/yellow</item>
        <item name="android:windowBackground">@color/blue_l</item>

        <item name="buttonBarStyle">@drawable/skew_btn</item>
    </style>
</resources>
